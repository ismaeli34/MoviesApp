#database configuration
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/movies?serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true


project:
  poster: posters/

base:
  url: "http://localhost:8080"